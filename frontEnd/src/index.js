import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/app'


ReactDOM.render(<App/>, document.querySelector('.container'));

/*En general todos los componentes y su estructura es muy clara y simple. Es un muy buen trabajo de dividir en componentes los elementos de la vista :) */
